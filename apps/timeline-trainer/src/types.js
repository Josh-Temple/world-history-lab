/**
 * @typedef {{ year_start: number }} EventTime
 * @typedef {{ id: string, label: string, time: EventTime, question_types: string[] }} EventRecord
 * @typedef {{ id: string, title: string, event_ids: string[] }} UnitRecord
 * @typedef {{ left: EventRecord, right: EventRecord, correctId: string }} BeforeAfterQuestion
 */

export {};
